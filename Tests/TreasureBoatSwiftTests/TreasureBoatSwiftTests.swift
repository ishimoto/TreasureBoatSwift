import XCTest
@testable import TreasureBoatSwift

final class TreasureBoatSwiftTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(TreasureBoatSwift().text, "Hello, World!")
    }
}
